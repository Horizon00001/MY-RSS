[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    unit: fast tests for isolated functions and classes
    integration: tests that exercise FastAPI routes or multiple modules together
    slow: tests that may take noticeably longer than the default unit suite
